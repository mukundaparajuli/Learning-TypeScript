const enum seatChoice {
  AISLE,
  MIDDLE_LEFT,
  MIDDLE_RIGHT,
  WINDOW,
}
const hcSeat = seatChoice.AISLE;

// we can provide certain options for the seatChoice
// the first value has 0 value initially
// we can provide values randomly to the data and the value for other will increase in an order
// we can use const to avoid scattered codes in js==> only the necessary codes will be generated

export {};
